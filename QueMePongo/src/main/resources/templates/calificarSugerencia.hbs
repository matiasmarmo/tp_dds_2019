{{#partial "contenido"}}
	<div class="btn-group-vertical" role="group" aria-label="Basic example">
		{{#each eventos}}
		dsfkjsdfkj
		{{id}}
		asdasd
		<a href="/eventos/sugerencias?id={{id}}">
       		<button type="button" class="btn btn-primary">{{descripcion}}</button>
       	</a>
 		{{/each}}
	</div>
{{/partial}} {{> layout.hbs}}